import PageHeader from "../common/PageHeader";

function Settings() {
  return (
    <PageHeader
      title="Settings"
      subtitle="Application users, roles, and system configuration"
    />
  );
}

export default Settings;